package io.github.springmediator.mediator.core;

/**
 * Marker interface for commands. Commands represent intent to change state.
 * A command has exactly one handler and returns void.
 */
public interface ICommand {
}
