# Default version for local development / mavenLocal builds.
# Real release builds get their version from -Pversion=<X.Y.Z> set by .github/workflows/release.yml,
# which reads the version from the tag pushed by release-please.
version=1.0.0-SNAPSHOT
